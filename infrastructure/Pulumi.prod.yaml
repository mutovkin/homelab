config:
  # EQ12 Proxmox connection
  homelab-infrastructure:eq12Endpoint: https://EQ12_PROXMOX_IP:8006
  homelab-infrastructure:eq12Node: pve

  # N5 Pro Proxmox connection
  homelab-infrastructure:n5proEndpoint: https://N5PRO_PROXMOX_IP:8006
  homelab-infrastructure:n5proNode: n5pro

  # Proxmox API credentials are set via:
  #   pulumi config set --secret proxmoxve:apiToken "USER@REALM!TOKENID=SECRET"
  # Or per-provider instance in code.
