# Portainer — templated by Ansible
# Docker network subnet (single source of truth: host_vars docker_networks)
PORTAINER_NETWORK_SUBNET={{ docker_networks.portainer | mandatory('docker_networks.portainer must be pinned in host_vars — see the fleet map in host_vars/n5pro_docker/vars.yml') }}
