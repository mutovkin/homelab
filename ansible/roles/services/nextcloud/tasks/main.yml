---
- name: Deploy NextCloud via the shared service pipeline
  ansible.builtin.include_role:
    name: services/_deploy
  vars:
    svc: nextcloud
    svc_data_dirs:
      - "{{ data_mount }}/nextcloud/data"
