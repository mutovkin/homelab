# PostgreSQL Client Authentication Configuration File
# Deployed by Ansible (roles/services/postgresql) — edit in the repo, not live.
# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Docker networks (172.16.0.0/12 covers all compose subnets incl. postgres_network 172.21.0.0/24)
host    all             all             172.16.0.0/12           scram-sha-256

# Catch-all — reproduces the pre-#78 effective behaviour (image default
# "host all all all scram-sha-256"). Tightening LAN exposure is a separate issue.
host    all             all             0.0.0.0/0               scram-sha-256

# Replication connections
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256
host    replication     all             172.16.0.0/12           scram-sha-256
