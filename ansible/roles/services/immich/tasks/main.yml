---
- name: Deploy Immich via the shared service pipeline
  ansible.builtin.include_role:
    name: services/_deploy
  vars:
    svc: immich
    svc_data_dirs:
      - "{{ data_mount }}/immich/upload"
