---
- name: Deploy Frigate via the shared service pipeline
  ansible.builtin.include_role:
    name: services/_deploy
  vars:
    svc: frigate
    svc_data_dirs:
      - "{{ data_mount }}/frigate/config"
      - "{{ data_mount }}/frigate/recordings"
