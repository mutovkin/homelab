---
# Beelink EQ12 (AZW) — host-specific variables
# CPU: Intel N100 (4c/4t, Alder Lake-N)
# RAM: 16GB (single DIMM)
# NVMe: 1× 2TB TEAMGROUP TM8FP4002T
# Net: 2× Intel I225-V 2.5GbE (enp2s0 → vmbr0)

# === NUT (Network UPS Tools) — client ===
# Monitors N5 Pro's NUT server and shuts down on battery low
nut_role: "client"
nut_client:
  monitor_ups_name: "goldenmate"
  monitor_host: "192.168.30.5"
  shutdowncmd: "/sbin/shutdown -h +0"
  powerdownflag: "/etc/killpower"

# Proxmox
proxmox_node: pve

# Hardware
total_ram_gb: 16

# Storage
zfs_pool: rpool
data_mount: /data

# Network
proxmox_bridge: vmbr0

# === VM/LXC Definitions (managed by proxmox_guests role) ===
# Ported from infrastructure/machines/eq12.ts

proxmox_vms:
  - vmid: 100
    name: ha
    cores: 2
    memory: 4096
    # Everything below is pinned to the LIVE VM (qm config 100, 2026-08-16) so a
    # from-scratch rebuild matches the running guest (#87). All of it is
    # create-time-only (provisioning is update:false, #86), so the live VM is
    # untouched by adding it here.
    balloon: 2048
    cpu: host
    # Quoted deliberately: the module param is type=str, and the YAML bool trap
    # (`agent: yes`) would send the string "True" instead of "1".
    agent: "1" # qemu-guest-agent runs inside HAOS
    scsihw: virtio-scsi-pci # live VM predates the role's virtio-scsi-single default
    scsi:
      scsi0: "local-zfs:32,discard=on,ssd=1,format=raw"
    # Pinned to live: HAOS boots UEFI, so recreating this VM with seabios would
    # not boot. The vars said seabios purely because nothing ever compared them
    # against the running guest (#86). No pre-enrolled secure-boot keys.
    bios: ovmf
    efidisk0:
      storage: local-zfs
      format: raw
      efitype: 4m
      pre_enrolled_keys: false
    # NO `machine:` — the live VM has no machine line at all (i440fx default).
    # The `machine: q35` that used to sit here never matched reality, and a q35
    # rebuild is an untested boot config for this HAOS install.
    usb:
      usb0: "host=1a86:55d4" # USB serial radio stick (CH9102)
      usb1: "host=10c4:ea60" # USB serial radio stick (CP2102)
    localtime: true
    tablet: false
    # Pins the MAC so a rebuilt VM keeps its DHCP reservation and IP. Declaring
    # `net` also enrolls VM 100 in the MAC-preserving NIC-reconcile task, where
    # desired == live makes it a no-op (simulated against the captured live
    # config before landing this). WARNING: because that task DOES reconcile,
    # editing this string later hot-rewrites the running HA VM's NIC.
    net:
      net0: "virtio=02:AB:9F:1F:CD:8C,bridge=vmbr0"
    # Not modelled (benign / non-reproducible): smbios1 uuid, vmgenid, meta,
    # snapshot parent, tteck description text. Boot order is deliberately left
    # to PVE: live carries an explicit `boot: order=scsi0`, while a rebuild gets
    # a device-derived order (VM 200, created by this role with no boot param,
    # came out as `order=scsi0;ide2;net0`) — scsi0 still first, with a net
    # fallback. The `tags` divergence is also deliberate: live carries only
    # proxmox-helper-scripts; home-assistant is added here on purpose.
    onboot: true
    started: true
    tags:
      - proxmox-helper-scripts
      - home-assistant
    description: "Home Assistant OS — QEMU VM"

proxmox_lxcs:
  - vmid: 101
    hostname: deb-docker
    ostemplate: "local:vztmpl/debian-12-standard_12.7-1_amd64.tar.zst"
    cores: 2
    memory: 4096
    root_disk_size: 24
    storage: local-zfs
    mounts:
      # Allocation form (like CT 201): `mounts` is consumed at CREATE only
      # (provisioning is pinned update:false, #86), so this line's only job is
      # to tell a from-scratch rebuild what to allocate — naming a subvol here
      # fails creation on a rebuilt host because the volume does not exist yet
      # (#87). Live volume: local-zfs:subvol-101-disk-1, 110G, backup=1
      # (pct config 101, 2026-08-16).
      # DR: a rebuild allocates a FRESH EMPTY /data — restore it from the vzdump
      # backup (backup=1 includes mp0) before deploying services. That ordering
      # is now ENFORCED (#127), not just documented: a run that creates this CT
      # writes /var/lib/ansible-fresh-allocation inside it (listing every mount it
      # allocated fresh — see `fresh_allocation_manifest` in group_vars/all), and
      # deploy-services.yml refuses to run for any docker host carrying it.
      # The arming survives an interrupted build (#148): the Proxmox host records
      # the intent to allocate BEFORE the container is created and clears it only
      # once the manifest is written, so a run killed in between is picked up by
      # the next one rather than leaving the gate silently unarmed.
      # Restore the volume, then `rm /var/lib/ansible-fresh-allocation` — that rm
      # is the explicit "yes, this data is what I want" acknowledgement, and it is
      # the only thing that clears the gate. (Each listed mount also gets a
      # `.fresh-allocation` breadcrumb at its root; that is evidence, not the
      # gate.) On a genuinely greenfield build, remove it without restoring.
      mp0: "local-zfs:110,mp=/data,backup=1"
    netif:
      net0: "name=eth0,bridge=vmbr0,gw=192.168.23.1,hwaddr=AE:E1:F1:EE:F5:9A,ip=192.168.25.15/18,type=veth"
    nesting: true
    onboot: true
    started: true
    tags:
      - docker
    description: "Primary Docker host — runs all container services (observability, postgresql, vaultwarden, etc.)"

  - vmid: 102
    hostname: ubuntu-docker
    ostemplate: "local:vztmpl/ubuntu-22.04-standard_22.04-1_amd64.tar.zst"
    cores: 1
    memory: 1024
    root_disk_size: 8
    storage: local-zfs
    netif:
      net0: "name=eth0,bridge=vmbr0,hwaddr=BC:24:11:C9:0F:0C,ip=dhcp,type=veth"
    nesting: true
    onboot: false
    started: false
    tags:
      - docker
    description: "Secondary Docker host (Ubuntu 22.04) — currently unused"

  - vmid: 104
    hostname: nginxproxymanager
    ostemplate: "local:vztmpl/debian-12-standard_12.7-1_amd64.tar.zst"
    cores: 2
    memory: 1024
    root_disk_size: 4
    storage: local-zfs
    netif:
      net0: "name=eth0,bridge=vmbr0,gw=192.168.23.1,hwaddr=5E:E1:3E:81:01:5F,ip=192.168.25.20/18,type=veth"
    nesting: false
    onboot: true
    started: true
    tags:
      - nginx-proxy-manager
    description: "Nginx Proxy Manager — reverse proxy and SSL management"

# --- telegraf_agent (#186) ---------------------------------------------------
# eq12 is the one host with ACPI fan objects (five, binary cur_state 0/1 —
# measured 2026-08-24; NOT tachometers, there is no RPM on this board).
telegraf_agent_acpi_fan_state: true
# How many of them, asserted for EQUALITY against VictoriaMetrics at the end of
# the deploy and reconciled against obs-fan-state-absent-eq12's threshold by a
# static guard in the observability role. Five ACPI fan objects, PNP0C0B:00-04,
# measured 2026-08-24 and confirmed 2026-08-25 as cooling_device3..7.
#
# THE COUNT IS THE POINT, not mere presence: files/acpi_fan_state.sh emits
# nothing for a device it cannot read rather than a fabricated zero, so a
# permission or sysfs change thins this 5 -> 4 silently. This number used to be
# hardcoded inside the role's assert; it lives here now so the alert rule and the
# deploy proof read ONE fact from ONE place.
telegraf_agent_acpi_fan_expected_count: 5
# Sensor temperature SERIES this host must deliver (not chips — five of the seven
# come from one chip). Measured 2026-08-25:
#   coretemp-isa-0000  package_id_0, core_0, core_1, core_2, core_3   (5)
#   acpitz-acpi-0      temp1                                          (1)
#   nvme-pci-0300      composite                                      (1)
# Stored names carry a digit (sensors_temp1_input .. sensors_temp5_input) because
# remove_numbers=false keeps core_0..3 from merging; there is no bare
# sensors_temp_input in the store.
#
# THE ALERT RULE COMPARES `lt`, NOT EQUALITY, and this host is why. A 7-day
# windowed count here returned seven steady series at 1050 samples each PLUS
# it8622-isa-0a30 temp1/temp2 at ONE sample each — the one-off forced it87
# binding then documented in docs/eq12.md. The set can grow with no change to
# this repo, so extra chips must be free and only losses may fire.
#
# it87 IS NOW PERSISTED, so this count rises 7 -> 11 in the same change, exactly
# as the note above demanded. The four added are it8622-isa-0a30's temp1..temp4.
#
# ONLY temperatures count here: the assert matches `sensors_temp<N>_input`, so
# the chip's other survivors — fan2, fan3 and pwm1..pwm5 — are NOT in this
# number despite arriving from the same input. Declaring 18 (every surviving
# feature) failed against a measured 11 on first apply. Those families are
# separate metric names and need their own owners, not a seat in this count.
telegraf_agent_expected_sensor_series: 11
# Substrings that MUST appear in `telegraf --test` output on this host — the
# input's OWN output, per CLAUDE.md's ping lesson. chip names measured 2026-08-24.
# Matched as LITERAL SUBSTRINGS, not as regexes: a `.` or `-` here is that
# character, nothing more.
telegraf_agent_expected_test_output:
  - "chip=coretemp-isa-0000" # 5 CPU temps (Package + Core 0..3)
  - "chip=acpitz-acpi-0"
  - "chip=nvme-pci-0300" # root NVMe Composite
  - "smart_device," # inputs.smart is actually producing
  - "acpi_fan_state," # the exec fan-state script is actually producing
  # RAPL (#194). Influx line protocol sorts tag keys, so `domain` precedes the
  # agent's `environment`/`host`/`location` — the substring is the line's head.
  # Verified against the script's real output on this host, 2026-08-25.
  # THESE ANCHOR THE LINE HEAD ONLY, and deliberately stop before the field set:
  # they prove the measurement and the domain label, and they can see NOTHING
  # about the fields after it. In particular a comma->space regression on
  # #206's energy_uj field still matches all three of these while silently
  # emitting one field stamped in 1970. The guard for that is the role's
  # VictoriaMetrics arrival assert on rapl_energy_uj, not this list.
  - "rapl,domain=package-0"
  - "rapl,domain=core"
  - "rapl,domain=uncore"
  # it87 fan tachometers (persisted 2026-08-25). These are the ONLY proof that
  # the forced binding produced the thing it exists for: the module can load,
  # the chip can appear in `sensors`, and the tachometer fields can still be
  # absent. Assert the FANS, not the chip.
  #
  # Bare `feature=` and not `chip=...,feature=...`, for the same reason the RAPL
  # entries below are written the way they are: influx line protocol SORTS tag
  # keys, and the agent's own `environment` tag sorts between `chip` and
  # `feature`. The pair is never contiguous, so the intuitive substring can
  # never match. Measured — this assert failed exactly that way on first apply,
  # before restarting telegraf, which is the guard doing its job.
  # No other chip on this host exposes fan2/fan3, so the bare form is unambiguous.
  - "feature=fan2"
  - "feature=fan3"
# The junk half of the same chip, proven ABSENT from the input's own output.
# Kept in sync with telegraf_agent_sensors_tagdrop_features by hand and on
# purpose — the role infers neither from the other (see that role's defaults).
telegraf_agent_forbidden_test_output:
  - "feature=3vsb"      # reads 600 mV on a 3.3 V rail — fabricated, not measured
  - "feature=intrusion0" # permanent phantom ALARM on a chassis with no switch
  - "feature=vbat"

# it8622-isa-0a30 offers 22 features; 11 are meaningless on this board and are
# dropped before they can reach VictoriaMetrics. The voltage rails (in0..in6,
# +3.3v, 3vsb, vbat) come off an uncalibrated divider whose board-specific
# scaling ITE never published — +3.3v reads 3.58 V and 3vsb reads 0.60 V, so the
# numbers are not merely imprecise, they are wrong. intrusion0 is a permanent
# ALARM on a chassis with no intrusion switch wired.
#
# NOTE WHAT IS *NOT* HERE: it87's temp1..temp4. This filter matches on FEATURE
# NAME ALONE, across every chip, and `temp1` is also acpitz-acpi-0's only
# feature — dropping it here would silently delete an unrelated working sensor.
# The four board thermistors are therefore KEPT, distinguishable by their chip
# tag, and the dashboard labels them as unidentified board sensors rather than
# claiming to know what they measure. Three of them read an identical 49 C,
# which is consistent with unconnected pins; that is a reason to label them
# honestly, not a reason to break acpitz.
telegraf_agent_sensors_tagdrop_features:
  - "3vsb"
  - "intrusion0"
  - "vbat"
  - "+3.3v"
  - "in0"
  - "in1"
  - "in2"
  - "in3"
  - "in4"
  - "in5"
  - "in6"

# --- Super-I/O fan tachometers (proxmox_host) --------------------------------
# The EQ12's ITE chip reports itself as IT8613E, which the in-kernel it87 driver
# does not list among its 20 supported IDs. force_id=0x8622 binds it as the
# register-compatible IT8622E and yields two REAL tachometers (measured
# 3688 / 1890 RPM, 2026-08-25). This is in-tree, so there is no DKMS treadmill
# and a kernel upgrade cannot orphan it.
#
# The cost: ACPI already claims I/O region 0x0a30, so the driver refuses to
# attach without ignore_resource_conflict=1. We are overriding a claim the
# firmware made, on every boot. Nothing on this board is known to drive that
# region from the ACPI side, and dmesg reports a clean bind with no conflict
# warning — but that is an absence of observed harm, not a proof of safety.
proxmox_host_hwmon_modules:
  - name: it87
    options: "force_id=0x8622 ignore_resource_conflict=1"
    expect: "it8622-isa-0a30"
# RAPL package power (#194). THREE domains on this N100, enumerated from the
# `name` files and not the node indices: intel-rapl:0 = package-0,
# intel-rapl:0:0 = core, intel-rapl:0:1 = uncore.
# max_energy_range_uj = 262143328850 on all three.
#
# `core` HERE IS A GENUINE ALL-CORE AGGREGATE, and it is exported for that
# reason — established by the same load-placement test that DISQUALIFIED n5pro's
# identically-named domain (see that host's vars for the confound this avoids:
# loading CPU0 alone cannot tell an aggregate from a CPU0-only counter).
# Measured 2026-08-25, 6 s windows:
#
#   placement                package W     core W
#   idle                      1.70          1.62
#   busy-loop on CPU0        11.47         11.39
#   busy-loop on CPU3        12.13         12.05
#   busy-loop on CPU1+2+3    19.55         19.46
#   idle again                1.86          1.78
#
# core tracks package to within ~0.08 W at EVERY placement, including cores that
# a CPU0-only counter could not see. That is the positive control for the n5pro
# negative: same sysfs path, same domain name, different board, opposite answer.
#
# uncore is the iGPU. Its counter DOES advance — measured 2026-08-25, 122 uJ over
# 10 s and 488 uJ over 60 s (~10 uW), and a 24 h max of 0.000243 W in
# VictoriaMetrics — so it is live rather than frozen or unsupported. At the
# script's ~1 s sample window the counter often does not tick at all (its quantum
# is 61.035 uJ), so a large minority of samples read exactly 0.000000 — 268 of
# 1440 in a 24 h census, 2026-08-29 (18.6%), NOT "most": a real reading of a
# headless idle iGPU. Census recorded because #206 needed it: it is why that
# frozen-counter alert covers package-0 only, and why this domain is checked with
# changes(rapl_energy_uj{domain="uncore"}[30m]) rather than a wattage threshold.
# NOT ESTABLISHED: whether it TRACKS a real render load. That needs the same
# load-placement discipline applied to /dev/dri (a VAAPI/render job on this box),
# which was not run — no GPU workload tool is installed and installing one is a
# host change. Do not upgrade this to "tracks iGPU power" without that test.
telegraf_agent_rapl_expected_domains: [package-0, core, uncore]
